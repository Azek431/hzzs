<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml：HZZS 应用清单文件。

    权限声明：
    - SYSTEM_ALERT_WINDOW：悬浮窗权限，用于在系统顶层显示控制面板

    应用配置：
    - 主题：Theme.HuoZaiZaiAssistant（Material 3 浅色无操作栏主题）
    - 入口 Activity：MainActivity（唯一 Activity）

    注意：
    - 当前不需要 INTERNET 权限（QQ/Telegram 链接通过系统浏览器打开）
    - 不需要 RECORD_AUDIO、CAMERA 等敏感权限
    - 不需要 STORAGE 权限（Android 13+ 已废弃）
    - 不需要 MEDIA_PROJECTION 权限（当前未接入屏幕采集）
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 悬浮窗权限：用于在系统顶层显示控制面板 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HuoZaiZaiAssistant">

        <!-- 主 Activity：应用的唯一入口页面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 免责声明 Activity -->
        <activity
            android:name=".ui.disclaimer.DisclaimerActivity"
            android:exported="false"
            android:theme="@style/Theme.HuoZaiZaiAssistant"
            android:windowSoftInputMode="adjustResize" />

        <!-- 视觉识别设置 Activity -->
        <activity
            android:name=".ui.settings.VisionSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.HuoZaiZaiAssistant"
            android:windowSoftInputMode="adjustResize" />

        <!-- 前台通知服务：保持悬浮窗不被系统回收 -->
        <service
            android:name=".service.OverlayNotificationService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- 无障碍自动操作服务 -->
        <service
            android:name=".service.AutoOperationService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false"
            android:label="火崽崽助手自动操作">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
