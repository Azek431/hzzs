<?xml version="1.0" encoding="utf-8"?>
<!--
    首页主布局：Material Design 3 底部导航栏风格。

    整体结构：
    1. FragmentContainerView → 承载首页内容和设置内容（通过底部导航栏切换）
    2. MaterialBottomNavigationView → 底部导航栏（首页 / 设置）

    设计原因：
    - 使用 MD3 底部导航栏替代独立 Activity 打开设置，用户体验更流畅
    - 首页内容和设置内容作为 Fragment 共存于同一个 Activity 内
    - 底部导航栏固定，切换时不丢失页面状态
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_background">

    <!-- 内容区域：承载首页 Fragment 和设置 Fragment -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/bottom_nav_height" />

    <!-- 底部导航栏 -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_nav_height"
        android:layout_gravity="bottom"
        android:background="@color/surface"
        app:elevation="8dp"
        app:itemActiveIndicatorStyle="@color/surface_container_high"
        app:itemIconTint="@color/brand_primary"
        app:itemTextColor="@color/brand_primary"
        app:menu="@menu/bottom_nav_menu" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
