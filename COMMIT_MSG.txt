feat: 合并 DetectionSource 协议、算法存储 v2、Shizuku 健康监测与捐赠修复

- DetectionSource: C++ enum (0-4) → JNI (IIIFFFFFZZI)V → Kotlin enum, end-to-end contract
- FilteredDetection: preserve original source through filter path
- InstalledAlgorithmStore v2: per-catalog versions/current.json, dual cache staging install, fixed migration
- ShizukuHealthCheck: split check()/checkLight(), commandChecked flag, session flag for downgrade avoidance
- Donation: restore IEF_DIAN button, merge duplicate MainActivity code, add IEF guard
- Host build tools: add vision_v3 sources to match CMakeLists.txt
- Native tests: add detection source contract tests

All static checks pass (423 project checks, 182 resource checks).
Unit tests pass. Build succeeds.