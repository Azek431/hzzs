# HZZS 本地视觉回归

项目根目录的 `.env.hzzs.local` 保存项目外测试素材目录，已加入 `.gitignore`。

PowerShell 脚本可读取：

```text
HZZS_TEST_ASSET_ROOT=D:\Code\AI\火崽崽\火崽崽奇妙屋\算法测试\测试图片
```

Python 回归需要 Python、Pillow、NumPy 和可用的 `g++`。Android 项目本身不依赖这些工具。
